import LandingPage from './(marketing)/landing/page';

/**
 * Root route → Marketing landing page.
 * "Your business, handled."
 * The front door. The app lives at /app.
 */
export default function Page() {
  return <LandingPage />;
}
